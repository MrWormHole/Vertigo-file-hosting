<br />
<br />
<br />
<% if !flash[:notice].blank? %>
  <div class = "alert alert-info">
    <ul>
      <li> <%= flash[:notice] %> </li>
    </ul>
  </div>
<% end %>

<br />
<%= link_to "New File", new_attachment_path, class: "btn btn-primary" %>
<br />
<br />

<table class = "table table-bordered table-striped">
  <thead>
  <tr>
    <th>Name</th>
    <th>Download Link</th>
    <th> </th>
  </tr>
  </thead>

  <tbody>
  <% @attachments.each do |attachment| %>

    <tr>
      <td><%= attachment.name %></td>
      <td><%= link_to "Download File", url_for(attachment.file) if attachment.file.attached? %></td>
      <td><%= button_to "Delete",  attachment, method: :delete, class: "btn btn-danger", confirm: "Are you sure that you wish to delete #{attachment.name}?" %></td>
    </tr>

  <% end %>
  </tbody>

</table>